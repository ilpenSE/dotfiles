This files aren't going to home folder.

These are must be symlinked to somewhere in root.

And you have to run symlinkall.sh with sudo
For example: `sugar_candy_theme.conf` must be symlinked into `/usr/share/sddm/themes/Sugar-Candy/theme.conf`
